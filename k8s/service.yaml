apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: observability
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
  - name: http
    port: 80
    targetPort: 5000